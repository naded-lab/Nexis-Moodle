[project]
name = "nexis-moodle-worker"
version = "1.0.0"
requires-python = ">=3.13"
dependencies = [
  "beautifulsoup4>=4.14,<5",
  "cryptography>=47,<48",
]

[dependency-groups]
dev = ["workers-py"]
